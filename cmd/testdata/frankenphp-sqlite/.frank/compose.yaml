# Generated by Frank — edit frank.yaml, not this file

networks:
    frank:
        driver: bridge
services:
    laravel.test:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        environment:
            - APP_ENV=local
            - APP_DEBUG=true
            - PSYSH_HOME=/var/www/html/storage/psysh
            - XDG_CONFIG_HOME=/var/www/html/storage/psysh
            - WWWUSER=${UID:-1000}
        networks:
            - frank
        ports:
            - 80:80
            - 443:443
            - 443:443/udp
        stdin_open: true
        tty: true
        volumes:
            - .:/var/www/html
            - .frank/certs/:/etc/certs/:ro
        working_dir: /var/www/html
    laravel.vite:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - sh
            - -c
            - '[ -d node_modules ] || npm install; npm run dev'
        container_name: laravel.vite
        depends_on:
            laravel.test:
                condition: service_started
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
            - COREPACK_ENABLE_DOWNLOAD_PROMPT=0
        healthcheck:
            disable: true
        image: frank-frankenphp-sqlite-laravel.test
        networks:
            - frank
        ports:
            - 5173:5173
        restart: unless-stopped
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
