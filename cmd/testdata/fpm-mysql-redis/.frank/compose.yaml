# Generated by Frank — edit frank.yaml, not this file

networks:
    frank:
        driver: bridge
services:
    laravel.test:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_healthy
        env_file:
            - .env
        environment:
            - APP_ENV=local
            - APP_DEBUG=true
            - PSYSH_CONFIG=/var/www/html/.psysh.php
            - PSYSH_HOME=/var/www/html/storage/psysh
            - WWWUSER=${UID:-1000}
        networks:
            - frank
        ports:
            - 5173:5173
        stdin_open: true
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    mysql:
        environment:
            MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
            MYSQL_DATABASE: ${DB_DATABASE}
            MYSQL_PASSWORD: ${DB_PASSWORD}
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
            MYSQL_USER: ${DB_USERNAME}
        healthcheck:
            interval: 1s
            retries: 30
            start_period: 30s
            test:
                - CMD
                - mysqladmin
                - ping
                - -h
                - localhost
                - -p${DB_PASSWORD}
            timeout: 3s
        image: mysql:latest
        networks:
            - frank
        ports:
            - 3306:3306
        volumes:
            - mysql_data:/var/lib/mysql
            - .frank/scripts/create-testing-database.sh:/docker-entrypoint-initdb.d/10-create-testing-database.sh
    nginx:
        build:
            context: .
            dockerfile: .frank/nginx.Dockerfile
        depends_on:
            - laravel.test
        networks:
            - frank
        ports:
            - 80:80
            - 443:443
        volumes:
            - .:/var/www/html
            - .frank/certs/:/etc/certs/:ro
    redis:
        healthcheck:
            interval: 1s
            retries: 30
            start_period: 30s
            test:
                - CMD
                - redis-cli
                - ping
            timeout: 3s
        image: redis:alpine
        networks:
            - frank
        ports:
            - 6379:6379
        volumes:
            - redis_data:/data
volumes:
    mysql_data:
        driver: local
    redis_data:
        driver: local
