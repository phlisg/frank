# Generated by Frank — edit frank.yaml, not this file

networks:
    frank:
        driver: bridge
services:
    laravel.migrate:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - /bin/bash
            - -lc
            - |
              set -e
              if [ ! -d vendor ]; then
                echo "[frank.migrate] composer install"
                composer install --no-interaction --prefer-dist --no-progress
              fi
              echo "[frank.migrate] artisan migrate --force"
              php artisan migrate --force
        container_name: laravel.migrate
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_healthy
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-fpm-mysql-redis-workers-laravel.test
        labels:
            frank.init: migrate
            frank.project: fpm-mysql-redis-workers
        networks:
            - frank
        restart: "no"
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    laravel.queue.default.1:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - php
            - artisan
            - queue:work
            - --queue=default
        container_name: laravel.queue.default.1
        depends_on:
            laravel.migrate:
                condition: service_completed_successfully
            laravel.test:
                condition: service_started
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-fpm-mysql-redis-workers-laravel.test
        labels:
            frank.project: fpm-mysql-redis-workers
            frank.worker: declared
            frank.worker.pool: default
            frank.worker.type: queue
        networks:
            - frank
        restart: unless-stopped
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    laravel.queue.default.2:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - php
            - artisan
            - queue:work
            - --queue=default
        container_name: laravel.queue.default.2
        depends_on:
            laravel.migrate:
                condition: service_completed_successfully
            laravel.test:
                condition: service_started
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-fpm-mysql-redis-workers-laravel.test
        labels:
            frank.project: fpm-mysql-redis-workers
            frank.worker: declared
            frank.worker.pool: default
            frank.worker.type: queue
        networks:
            - frank
        restart: unless-stopped
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    laravel.queue.high.1:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - php
            - artisan
            - queue:work
            - --queue=high
        container_name: laravel.queue.high.1
        depends_on:
            laravel.migrate:
                condition: service_completed_successfully
            laravel.test:
                condition: service_started
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-fpm-mysql-redis-workers-laravel.test
        labels:
            frank.project: fpm-mysql-redis-workers
            frank.worker: declared
            frank.worker.pool: high
            frank.worker.type: queue
        networks:
            - frank
        restart: unless-stopped
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    laravel.test:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_healthy
        env_file:
            - .env
        environment:
            - APP_ENV=local
            - APP_DEBUG=true
            - PSYSH_CONFIG=/var/www/html/.psysh.php
            - PSYSH_HOME=/var/www/html/storage/psysh
            - WWWUSER=${UID:-1000}
        networks:
            - frank
        stdin_open: true
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    mysql:
        environment:
            MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
            MYSQL_DATABASE: ${DB_DATABASE}
            MYSQL_PASSWORD: ${DB_PASSWORD}
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
            MYSQL_USER: ${DB_USERNAME}
        healthcheck:
            interval: 1s
            retries: 30
            start_period: 30s
            test:
                - CMD
                - mysqladmin
                - ping
                - -h
                - localhost
                - -p${DB_PASSWORD}
            timeout: 3s
        image: mysql:latest
        networks:
            - frank
        ports:
            - 3306:3306
        volumes:
            - mysql_data:/var/lib/mysql
    nginx:
        build:
            context: .
            dockerfile: .frank/nginx.Dockerfile
        depends_on:
            - laravel.test
        networks:
            - frank
        ports:
            - 80:80
            - 443:443
            - 5173:5173
        volumes:
            - .:/var/www/html
    redis:
        healthcheck:
            interval: 1s
            retries: 30
            start_period: 30s
            test:
                - CMD
                - redis-cli
                - ping
            timeout: 3s
        image: redis:alpine
        networks:
            - frank
        ports:
            - 6379:6379
        volumes:
            - redis_data:/data
volumes:
    mysql_data:
        driver: local
    redis_data:
        driver: local
