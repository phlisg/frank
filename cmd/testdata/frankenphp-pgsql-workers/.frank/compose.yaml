# Generated by Frank — edit frank.yaml, not this file

networks:
    frank:
        driver: bridge
services:
    laravel.test:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        depends_on:
            mailpit:
                condition: service_healthy
            pgsql:
                condition: service_healthy
        env_file:
            - .env
        environment:
            - APP_ENV=local
            - APP_DEBUG=true
            - PSYSH_CONFIG=/var/www/html/.psysh.php
            - PSYSH_HOME=/var/www/html/storage/psysh
            - WWWUSER=${UID:-1000}
        networks:
            - frank
        ports:
            - 80:80
            - 443:443
            - 443:443/udp
            - 5173:5173
        stdin_open: true
        tty: true
        volumes:
            - .:/var/www/html
            - .frank/certs/:/etc/certs/:ro
        working_dir: /var/www/html
    mailpit:
        healthcheck:
            interval: 1s
            retries: 30
            start_period: 30s
            test:
                - CMD
                - wget
                - --no-verbose
                - --spider
                - http://127.0.0.1:8025/api/v1/info
            timeout: 3s
        image: axllent/mailpit:latest
        networks:
            - frank
        ports:
            - 1025:1025
            - 8025:8025
    migrate:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - /bin/bash
            - -lc
            - |
              set -e
              if [ ! -d vendor ]; then
                echo "[frank.migrate] composer install"
                composer install --no-interaction --prefer-dist --no-progress
              fi
              echo "[frank.migrate] artisan migrate --force"
              php artisan migrate --force
        depends_on:
            mailpit:
                condition: service_healthy
            pgsql:
                condition: service_healthy
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-frankenphp-pgsql-workers-laravel.test
        labels:
            frank.init: migrate
            frank.project: frankenphp-pgsql-workers
        networks:
            - frank
        restart: "no"
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    pgsql:
        environment:
            POSTGRES_DB: ${DB_DATABASE}
            POSTGRES_PASSWORD: ${DB_PASSWORD}
            POSTGRES_USER: ${DB_USERNAME}
        healthcheck:
            interval: 1s
            retries: 30
            start_period: 30s
            test:
                - CMD-SHELL
                - pg_isready -U ${DB_USERNAME:-sail}
            timeout: 3s
        image: postgres:latest
        networks:
            - frank
        ports:
            - 5432:5432
        volumes:
            - pgsql_data:/var/lib/postgresql/data
            - .frank/scripts/create-testing-database.sql:/docker-entrypoint-initdb.d/10-create-testing-database.sql
    queue.default.1:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - php
            - artisan
            - queue:work
            - --queue=default
        depends_on:
            laravel.test:
                condition: service_started
            migrate:
                condition: service_completed_successfully
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-frankenphp-pgsql-workers-laravel.test
        labels:
            frank.project: frankenphp-pgsql-workers
            frank.worker: declared
            frank.worker.pool: default
            frank.worker.type: queue
        networks:
            - frank
        restart: unless-stopped
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    queue.default.2:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - php
            - artisan
            - queue:work
            - --queue=default
        depends_on:
            laravel.test:
                condition: service_started
            migrate:
                condition: service_completed_successfully
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-frankenphp-pgsql-workers-laravel.test
        labels:
            frank.project: frankenphp-pgsql-workers
            frank.worker: declared
            frank.worker.pool: default
            frank.worker.type: queue
        networks:
            - frank
        restart: unless-stopped
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
    schedule:
        build:
            args:
                WWWGROUP: ${GID:-1000}
            context: .
            dockerfile: .frank/Dockerfile
        command:
            - php
            - artisan
            - schedule:work
        depends_on:
            laravel.test:
                condition: service_started
            migrate:
                condition: service_completed_successfully
        env_file:
            - .env
        environment:
            - WWWUSER=${UID:-1000}
        healthcheck:
            disable: true
        image: frank-frankenphp-pgsql-workers-laravel.test
        labels:
            frank.project: frankenphp-pgsql-workers
            frank.worker: declared
            frank.worker.type: schedule
        networks:
            - frank
        restart: unless-stopped
        stop_signal: SIGKILL
        tty: true
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
volumes:
    pgsql_data:
        driver: local
