# Generated by Frank — edit frank.yaml, not this file
FROM frank/runtime:{{.PHPVersion}}-frankenphp-node24-pg17

# The build context is .frank/ itself, not the project root: the app is bind-mounted
# at runtime, so shipping a 400MB vendor/+node_modules/ context to the daemon on
# every build bought nothing but minutes of tar.
COPY Caddyfile /etc/caddy/Caddyfile
